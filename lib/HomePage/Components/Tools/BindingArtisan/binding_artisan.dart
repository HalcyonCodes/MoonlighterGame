

import 'package:flutter/material.dart';

class BindingArtisan extends StatelessWidget {
  const BindingArtisan({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}